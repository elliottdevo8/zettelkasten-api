# Web framework
fastapi>=0.115.0
uvicorn>=0.30.0

# Testing (Phase 2)
pytest>=8.0
httpx>=0.27

# MCP server (Phase 4)
mcp>=1.0

# Auth (Phase 5)
python-dotenv>=1.0.0

# Auto-capture (Phase 6)
feedparser>=6.0
requests>=2.31
